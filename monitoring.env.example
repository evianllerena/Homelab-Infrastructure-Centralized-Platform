# Copy to a root-owned file outside the repository.
# chmod 600 /etc/homelab-monitoring/*.env

PUSH_URL=https://monitoring.example.invalid/api/push/REPLACE_ME
RESTIC_REPOSITORY=/mnt/backup/restic-repository
RESTIC_PASSWORD_FILE=/etc/restic/password
